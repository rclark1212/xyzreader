<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/tools"
    android:id="@+id/card_view"
    android:layout_gravity="center"
    card_view:cardCornerRadius="4dp"
    android:elevation="@dimen/selected_card_elevation"
    android:layout_width="@dimen/detail_cardview_width"
    android:layout_height="@dimen/detail_cardview_height">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar_cardview"
            android:layout_width="match_parent"
            android:layout_height="?actionBarSize"
            android:theme="@style/ToolbarDetail"
            style="?android:attr/textAppearanceMedium"
            android:elevation="@dimen/toolbar_elevation"
            app:contentInsetStart="@dimen/toolbar_text_start_margin"
            android:contentDescription="@string/app_name"
            app:layout_collapseMode="pin" />

        <android.support.v4.view.ViewPager
            android:id="@+id/pager"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </LinearLayout>
</android.support.v7.widget.CardView>
